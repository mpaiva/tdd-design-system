import { Meta } from '@storybook/blocks';
import { theme } from '../../tokens';

<Meta title="Tokens/Spacing" />

# Spacing Scale

Our spacing system uses a consistent scale to maintain visual rhythm throughout the interface.

{Object.entries(theme.spacing).map(([key, value]) => (
  <div key={key} style={{ marginBottom: '1rem' }}>
    <div style={{ 
      width: value, 
      height: '20px', 
      background: '#0284c7',
      marginBottom: '0.5rem' 
    }} />
    <code>{key}: {value}</code>
  </div>
))}